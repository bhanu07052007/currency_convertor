The currency converter is the project done by calling the required API's ,i handled them correctly in the code but the reason the converter is not working is the API isn't working.
I will update the base url once i found the free currency coverter API.
I haven't used any type of frame work for css and i just done it in the .css file.
